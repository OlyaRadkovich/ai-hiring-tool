# -*- coding: utf-8 -*-
from google.adk.agents import Agent

agent_4_data_extractor = Agent(
    name="interview_data_extractor",
    model="gemini-2.0-flash-exp",
    description="Агент для извлечения аудио из видео и его транскрипции.",
    instruction="""
Твоя задача — получить текст транскрипции интервью.

Входные данные:
- В контексте тебе будет предоставлен полный текст транскрипции интервью под ключом 'transcription_text'.

Действия:
1.  **Проверка и очистка:** Убедись, что текст транскрипции не пустой. Если необходимо, выполни базовую очистку (удаление лишних пробелов).
2.  **Форматирование вывода:** Верни полученный текст транскрипции как есть, без каких-либо изменений, JSON-объектов или дополнительной разметки. Твой вывод должен быть исключительно текстом транскрипции.

Это критически важный первый шаг в анализе интервью, поэтому точность и полнота возвращаемых данных имеют первостепенное значение.
""",
    tools=[],
)